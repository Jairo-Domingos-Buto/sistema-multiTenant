## **Sistema: API SaaS de Gestão de Tarefas com Times (multi-tenant)**

**Objetivo: parar de ser dependente**

Foco único: **Backend Laravel profundo**

### O que vais fazer:

- construir API do zero com:
    - auth (login, register, reset)
    - roles/permissões
    - CRUD completo
- usar:
    - Service layer (nada de lógica no controller)
    - Requests para validação
    - Resource para resposta

### Regras:

- sem IA nos primeiros 45 min
- commits diários
- nada de frontend complexo
